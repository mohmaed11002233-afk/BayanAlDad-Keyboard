name: Build Android App

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build with Gradle
        run: |
          mkdir -p app/src/main/java/com/bayandalad/keyboard
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/xml
          
          if [ -f "BayanKeyboardService.kt" ]; then
            cp BayanKeyboardService.kt app/src/main/java/com/bayandalad/keyboard/
          fi
          if [ -f "bayan_keyboard_layout.xml" ]; then
            cp bayan_keyboard_layout.xml app/src/main/res/layout/
          fi

          echo "include ':app'" > settings.gradle
          chmod +x ./gradlew || true
          
          cat << 'EOF' > build.gradle
          allprojects {
              repositories {
                  google()
                  mavenCentral()
              }
          }
          EOF

          cat << 'EOF' > app/build.gradle
          plugins {
              id 'com.android.application'
              id 'org.jetbrains.kotlin.android'
          }
          android {
              namespace 'com.bayandalad.keyboard'
              compileSdk 34
              defaultConfig {
                  applicationId 'com.bayandalad.keyboard'
                  minSdk 24
                  targetSdk 34
                  versionCode 1
                  versionName '1.0'
              }
          }
          EOF

          gradle wrapper
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: BayanAlDad-APK
          path: app/build/outputs/apk/debug/app-debug.apk
